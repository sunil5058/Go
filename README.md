# Leafline e-book platform

A focused Go e-book catalog with a lightweight browser interface and JSON API.

Run it with `go run .`, then open `http://localhost:8080`.

- `GET /api/books` — catalog
- `GET /api/books?q=go` — catalog search
- `GET /api/books/1` — a single book
